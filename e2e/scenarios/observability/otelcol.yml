receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
exporters:
  file:
    path: /reports/otel-traces.json
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
